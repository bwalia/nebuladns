on:
  push:
    branches: [ dummy ]
  pull_request:
    branches: [ dummy ]
  workflow_dispatch:
  
name: Deploy odincm to the dev, acc and prod environment
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: self-hosted
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - name: Check out this repo code
      uses: actions/checkout@v2

    - name: Run script file
      run: |
         chmod +x ./devops/scripts/build-and-deploy.sh
         ./devops/scripts/build-and-deploy.sh
      shell: bash

    - name: short-sha
      uses: benjlevesque/short-sha@v1.2
      with:
        length: 6

    - run: echo $SHA
      env:
       SHA: ${{ steps.short-sha.outputs.sha }}

    - run: echo $SHA
      env:
       SHA: ${{ env.SHA }}
     
 # Build and push docker image to customer AWS ECR
 #   - name: Build, tag, and push WebImpetus ci4 image to Amazon ECR
 #     run: |
 #       docker build -f devops/docker/Dockerfile -t 123154119074.dkr.ecr.eu-west-2.amazonaws.com/tenthmatrix/webimpetus-ci4:${{ env.SHA }} .
 #       docker push 123154119074.dkr.ecr.eu-west-2.amazonaws.com/tenthmatrix/webimpetus-ci4:${{ env.SHA }}

 #   - name: helm deploy
 #     uses: koslib/helm-eks-action@master
 #     env:
 #       KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
 #     with:
 #       command: helm upgrade -i ci4baseimagetest ./devops/webimpetus-chart --set=image.tag=${{ env.SHA }}
 
# throws 502 so test before enabling again    - name: Github Action for curl to test if deployment is successful for https://odincm.fictionally.xyz/
#      uses: wei/curl@v1.1.1
#      with:
#        args: https://odincm.fictionally.xyz/

    # - name: Github Action for curl
    #   uses: wei/curl@v1.1.1
    #   with:
    #     args: -X POST https://httpbin.org/post
        
    - name: Slack Notification
      uses: rtCamp/action-slack-notify@v2
      env:
        SLACK_CHANNEL: general
        SLACK_COLOR: ${{ job.status }} # or a specific color like 'good' or '#ff00ff'
        SLACK_ICON: https://github.com/rtCamp.png?size=48
        SLACK_MESSAGE: 'Post Content :rocket:'
        SLACK_TITLE: Post Title
        SLACK_USERNAME: rtCamp
        SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}

