apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odincm.fullname" . }}-test-connection"
  labels:
    {{- include "odincm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odincm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
