apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odincm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "odincm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odincm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
