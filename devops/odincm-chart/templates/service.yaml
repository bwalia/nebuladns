apiVersion: v1
kind: Service
metadata:
  name: {{ include "odincm-chart.fullname" . }}
  labels:
    {{- include "odincm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "odincm-chart.selectorLabels" . | nindent 4 }}
