//! High-level DNS message types for NebulaDNS.
//!
//! This crate contains pure data types (no I/O). The wire codec lives in `nebula-wire`.

#![forbid(unsafe_code)]

/// Placeholder until M1.
pub const MARKER: &str = "nebula-proto";
